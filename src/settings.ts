const settings = {
  language: process.env.SPEECH_SDK_LANGUAGE,
  serviceRegion: process.env.SPEECH_SDK_SERVICE_REGION,
  subscriptionKey: process.env.SPEECH_SDK_SUBSCRIPTION_KEY,
};

export default settings;